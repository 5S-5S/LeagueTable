name = "leaguetable-api"
main = "src/index.js"
compatibility_date = "2025-09-01"

# Created via `wrangler d1 create leaguetable` (see backend/worker/README.md).
# database_id gets filled in once that command has been run.
[[d1_databases]]
binding = "DB"
database_name = "leaguetable"
database_id = ""
